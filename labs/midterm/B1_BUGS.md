# B1 · บันทึกการแก้บั๊ก 6 จุด — สอบกลางภาค ENGSE203

**ชื่อ–นามสกุล:** นายธนกร กองใจ **รหัส:** 68543210026-9 **Sec:** 1 **ชุด:** A

| # | อาการ (ให้มาแล้ว) | ไฟล์ : บรรทัด | สาเหตุ (ทำไมผิด) | แก้อย่างไร |
|---|---|---|---|---|
| 1 | Console เตือนสีเหลืองเรื่องรายการ (list) | RequestList.jsx : 9 | ใน function ที่ใช้ .map ไม่มี key เพื่อระบุตัวตน | เพิ่ม key={request.id} ด้านหน้า request={request} |
| 2 | ตัวเลข "รอดำเนินการ" ในแผงสรุปไม่ตรงกับที่เห็นจริง | DashboardPage.jsx : 47 | ในตัวแปร summary บรรทัด pending: โค้ดไปนับสถานะ completed แทนที่จะเป็น pending | เปลี่ยนแค่คำว่า completed ของบรรทัด pending เป็น pending |
| 3 | กดตัวกรอง "รอดำเนินการ" แล้วได้รายการที่ไม่ใช่ | DashboardPage.jsx : 55 | filter ใช้สัญลักษณ์ในการเก็บข้อมูลผิด แทนที่จะเป็น === แต่ใช้ !== ซึ่งหมายความว่าเอา request ที่ไม่ตรงกับ statusFilter ใส่เข้าไปทั้งหมด | เปลี่ยนเป็น === |
| 4 | เปลี่ยน URL REQ-001 → REQ-002 แล้วข้อมูลไม่เปลี่ยน | RequestDetailPage.jsx : 28 | ไม่มีตัว requestId ทำให้เวลาเปลี่ยนข้อมูล react ไม่ยอมดึงข้อมูลใหม่ | เพิ่ม requestId |
| 5 | กด "ลบ" แล้วรายการยังอยู่ ต้องรีเฟรชถึงหาย | DashboardPage.jsx : 65 | setRequests(requests) ซึ่งเป็นการบอกให้ React อัปเดตหน้าจอด้วยตัวแปรชุดเก่า | เปลี่ยน requests เป็น nextRequests |
| 6 | กด "Reset Demo Data" แล้วหน้าพัง/ว่างเปล่า | DashboardPage.jsx : 65 | ใน function handleReset ที่มี async นำหน้าแต่กลับไม่มี await เพื่อรอให้หลังบ้านส่งข้อมูลกลับมาทำจึงทำให้หน้าเว็บขาว | เพิ่ม await หน้า resetRequests() |
