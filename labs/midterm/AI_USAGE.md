# AI Usage Log — สอบกลางภาค

ชื่อ-รหัส: ___นายธนกร กองใจ___68543210026-9___

บันทึกทุกครั้งที่ใช้ AI ระหว่างสอบ

| เวลา | งาน (B1/B2/B3/B4) | ถาม AI ว่าอะไร | ใช้คำตอบส่วนไหน | แก้เอง/ตรวจสอบอย่างไร |
|---|---|---|---|---|
| 11:06 | B1.1 | สาเหตุมาจาก requestList ใช่มั้ย function RequestList ปัญหาอยู่ตรงไหน เป็นเพราะไม่ได้ใส่ key หรอ | สาเหตุคือการใช้ฟังก์ชัน .map() วนลูปสร้าง Component ใน React บังคับว่าแต่ละ Component ย่อยต้องมี key ที่ไม่ซ้ำกันเพื่อใช้ระบุตัวตน | วิธีตรวจสอบ หาบรรทัดที่ใช้ .map ดูว่าได้ใส่ key มั้ย ถ้าไม่ก็เติม key |
| 11:56 | B1.4 | RequestDetailPage ผิดพลาดตรงไหนทำไมเวลาเปลี่ยน req บน url แล้วหน้าเว็บไม่เปลี่ยนตาม | สาเหตุหลักที่ข้อมูลไม่ยอมเปลี่ยนตาม URL มักเกิดจากฟังก์ชัน useEffect ที่ทำหน้าที่ดึงข้อมูล ไม่ได้ใส่ตัวแปร ID (ที่ดึงมาจาก URL) เอาไว้ใน Dependency Array [] ตอนท้ายของฟังก์ชัน | ไปหาดูท้ายฟังชั่นตามที่ AI บอกและเพิ่ม requestID ด้านล่างสุด |
| 12:00 | B1.5 | อยู่ตรง handleDelete ใช่มั้ย ต้องแก้ตรงไหนเพราะอะไรอธิบายหน่อย | setRequests(requests) ซึ่งเป็นการบอกให้ React อัปเดตหน้าจอด้วยตัวแปรชุดเก่า | เปลี่ยนตัวแปรในวงเล็บจาก requests เป็น nextRequests |
| 12:20 | B2.1 | ช่วยทำแท็ก <input> หน่อย, คำสั่ง onChange คืออะไร | เวลาคุณพิมพ์ตัวอักษรลงในช่อง <input> คำสั่ง onChange จะเรียกใช้ setSearchQuery เพื่อเอาข้อความล่าสุดไปเก็บไว้ พอ React เห็นว่าข้อมูลในกล่องความจำเปลี่ยน มันก็จะทำการอัปเดตหน้าจอให้แสดงตัวอักษรนั้นทันที | เขียนและเพิ่มตัวแปรสำหรับ search เอง กำหนดชื่อตัวแปรเป็น searchQuery และมี setSearchQuery สำหรับเปลี่ยนแปลงค่า |
| 12:44 | | | | |
| | | | | |
| | | | | |