# LAB05 · Checkpoint Cards — คาบ 5A (Read Path)

ใช้วงจร `ทาย → แก้ → รัน → สังเกต → อธิบาย → ผ่าน` ทุกช่วง
**ทำเฉพาะ TODO ที่ขึ้นต้นด้วย `5A-` เท่านั้น** ส่วน `5B-` เป็นงานของคาบหน้า

| CP | เวลา | โหมด | สิ่งที่ต้องทำ | ผ่านเมื่อ |
|---|---:|---|---|---|
| **CP00** | 20 | ทำเอง | `npm ci` · `npm run dev` · ทดสอบ add/filter/delete/validation ของเดิม | 4 พฤติกรรมทำงาน และเข้าใจว่าทำไม refresh แล้วข้อมูลใหม่หาย |
| **CP01** | 35 | ทำด้วยกัน → ทำเอง | ย้าย state และ UI ของ Dashboard จาก `App.jsx` ไป `DashboardPage.jsx` | หน้าจอเหมือนเดิมทุกพิกเซล และ `App.jsx` ไม่ถือ state ของ feature |
| **CP02** | 45 | ทำด้วยกัน 1 route → ทำเอง 4 route | `<Outlet />` · route ครบ 5 เส้น · NavLink 3 ปุ่ม | เปิดครบทั้ง 5 URL และ Network ไม่โหลด `.html` ใหม่ |
| **CP03** | 70 | ทำด้วยกัน 2 ช่วง → ทำเอง 3 ช่วง | Service โหลด seed · Effect · 5 สถานะ · ปุ่มลองใหม่ | `#/` `#/?scenario=error` `#/?scenario=empty` ทำงานครบ |
| **CP05a** | 30 | ทำเองล้วน | `useParams()` · `getRequestById()` · แยกพบ/ไม่พบ | `REQ-001` แสดงข้อมูล · `REQ-999` แสดง "ไม่พบคำร้อง" |

## CP03 แบ่งเป็น 5 ช่วงย่อย — รันหลังจบทุกช่วง

| ช่วง | นาที | ทำอะไร | จบแล้วต้องเห็น |
|---|---:|---|---|
| CP03-1 | 15 | `TODO 5A-1` fetchSeedRequests + ตรวจ `response.ok` | `console.log` ได้ข้อมูล 3 รายการ |
| CP03-2 | 25 | `TODO 5A-2` + state และ Effect ใน Dashboard (เฉพาะกรณีสำเร็จ) | รายการขึ้นหน้าจอ |
| CP03-3 | 15 | เพิ่ม loading / error / empty | 3 สถานะแสดงถูก |
| CP03-4 | 10 | ปุ่มลองใหม่ (ใช้ `useManualReload` ที่ให้มาแล้ว) | กดแล้วกลับมาปกติ |
| CP03-5 | 5 | เก็บภาพหน้าจอ 3 ภาพ | ไฟล์ครบใน `evidence/images/` |

> เขียน 100 บรรทัดรวดเดียวแล้วค่อยรัน = เจอ error 5 ตัวพร้อมกัน แยกไม่ออกว่าตัวไหนทำให้พัง
> **นี่ไม่ใช่คำแนะนำ แต่เป็นกติกา**

## คำถามที่ต้องตอบได้ก่อนผ่านแต่ละ CP

| CP | คำถาม |
|---|---|
| CP01 | ทำไมหน้าจอไม่ควรเปลี่ยนเลยหลังย้ายโค้ด |
| CP02 | `<Outlet />` ทำหน้าที่อะไร |
| CP03 | ทำไม loading กับ empty ถึงไม่ใช่สถานะเดียวกัน |
| CP05a | `#/requests/REQ-999` ต่างจาก `#/xyz` อย่างไร |

## กติกา

- แก้เฉพาะ TODO ของ CP ปัจจุบัน อย่าข้ามไปทำของ CP ถัดไป
- หลังแก้ทุกครั้ง รัน `npm run dev` แล้ว `npm run check -- --session=1`
- รายการที่ขึ้น `[SKIP]` คืองานของคาบ 5B ยังไม่ต้องทำ
- ติดที่เดิมเกิน 10 นาที ให้ยกมือ — มีทางลงเตรียมไว้ให้แล้ว
- ต้องอธิบายโค้ดของตัวเองได้ ถ้าใช้ AI ช่วย ต้องบันทึกใน `AI_USAGE.md`

## จบคาบนี้ต้องได้

```
npm run check -- --session=1     →  ผ่าน 104/104 รายการ
```

**ทำงานได้แล้ว** — route 5 เส้น · โหลดข้อมูลผ่าน Service · 5 สถานะ · หน้ารายละเอียดตามรหัส · ตัวกรองและแผงสรุป

**ยังทำไม่ได้ (คาบหน้า)** — เพิ่ม ลบแบบถาวร คืนค่าเริ่มต้น ข้อมูลอยู่ต่อหลัง refresh กู้คืนข้อมูลเสียหาย
